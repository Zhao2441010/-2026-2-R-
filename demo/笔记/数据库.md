每个类就是对应数据库的一张表
通过  单表继承   等三种方式  实现继承
通过  @OneToMany  @ManyToOne  实现一对多的表

类和sql封装在reposity

service通过reposity来实现数据操纵


JPA单表继承:


2.数据库

在yml配置JPA后,加上@Entity和@Id等会自动建表

    数据库配置,在src/main/resources/application.yml配置取代conn={}
    
    数据库操作接口写在src\main\java\com\example\demo\repository

        @Repository
        public interface CatRepository extends JpaRepository<Cat, Long> {
        }

    增删改查在src\main\java\com\example\demo\server
    通过调用接口实现

        @Autowired
        private CatRepository catRepository;

        // 查
        public List<Cat> list() {
            return catRepository.findAll();
        }

    创建表//初始化
    1.在mysql直接建表
    2.在src/main/resources/schema.sql写
    3.在server里面写

3.多线程
mysql自己就支持并行查询    多线程只需要多用户处理
sqlite不支持              多线程处理多用户和上锁多查询
